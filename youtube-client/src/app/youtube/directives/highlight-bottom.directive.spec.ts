import { HighlightBottomDirective } from './highlight-bottom.directive';

describe('HighlightBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightBottomDirective();
    expect(directive).toBeTruthy();
  });
});
